from ownCustomPackage.ownSubPackage.mult import multiplication
m = multiplication(9,5)

print(m)

# ownCustomPackage -> is our own custom created package
# ownSubPackages -> is subpackage under created ownCustomPackage